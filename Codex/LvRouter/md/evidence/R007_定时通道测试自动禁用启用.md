# 主张
- R007：渠道测试可定时触发；测试超时或错误可自动禁用，满足条件可自动启用。

# 证据
- 文件：`internal/app/app.go`
  - 函数/符号：`Run`
  - 行号范围：L77-L83
  - 片段概述：读取 `CHANNEL_TEST_FREQUENCY` 并启动 `AutomaticallyTestChannels`。
- 文件：`internal/admin/controller/channel/test.go`
  - 函数/符号：`testChannels` / `AutomaticallyTestChannels`
  - 行号范围：L230-L271 / L317-L323
  - 片段概述：响应时间超阈值触发禁用；错误触发 `ShouldDisableChannel`；满足条件时 `ShouldEnableChannel` 重新启用。
- 文件：`internal/admin/monitor/manage.go`
  - 函数/符号：`ShouldEnableChannel`
  - 行号范围：L46-L56
  - 片段概述：无错误且配置允许时可启用。

# 结论
- 健康测试为“主动探测”，可影响渠道启用/禁用状态。

