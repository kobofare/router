# 主张
- R010：某些路径未显式提供 model 时会自动补全默认模型名称。

# 证据
- 文件：`internal/transport/http/middleware/utils.go`
  - 函数/符号：`normalizeRelayPath` / `getRequestModel`
  - 行号范围：L24-L58
  - 片段概述：`/api/v1/public/*` 归一化到 `/v1/*`；moderations/embeddings/images/audio 路径补全默认模型。

# 结论
- “请求模型”可能被自动补全，从而影响选渠维度。

