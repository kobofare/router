# 主张
- R003：开启内存缓存时，渠道按 priority 分层，随机选择最高优先级；重试可跳过最高优先级。

# 证据
- 文件：`internal/admin/model/cache.go`
  - 函数/符号：`CacheGetRandomSatisfiedChannel`
  - 行号范围：L228-L255
  - 片段概述：先按 priority 切分最高优先级区间，再 `rand.Intn(endIdx)` 随机选取；`ignoreFirstPriority` 时从低优先级区间选取。

# 结论
- 同优先级内随机分配；重试时允许降级到次优先级渠道。

