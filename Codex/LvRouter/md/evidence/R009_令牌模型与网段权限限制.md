# 主张
- R009：TokenAuth 对 token 的 subnet 与 models 白名单进行约束，违规即拒绝。

# 证据
- 文件：`internal/transport/http/middleware/auth.go`
  - 函数/符号：`TokenAuth`
  - 行号范围：L208-L220 / L264-L276 / L302-L329
  - 片段概述：若 subnet 不匹配 IP 则拒绝；若 models 未包含请求模型则拒绝。

# 结论
- token 自带“网段 + 模型”权限边界，影响请求是否可继续路由。

