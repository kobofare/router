# 主张
- R006：请求失败后，若错误满足规则则自动禁用渠道；否则记录指标，成功率低于阈值时自动禁用。

# 证据
- 文件：`internal/admin/controller/relay.go`
  - 函数/符号：`processChannelRelayError`
  - 行号范围：L130-L137
  - 片段概述：满足 `ShouldDisableChannel` 则 `DisableChannel`，否则 `monitor.Emit`。
- 文件：`internal/admin/monitor/manage.go`
  - 函数/符号：`ShouldDisableChannel`
  - 行号范围：L11-L43
  - 片段概述：根据 statusCode / error type / error code / message 决定是否禁用。
- 文件：`internal/admin/monitor/metric.go`
  - 函数/符号：`consumeFail` / `Emit`
  - 行号范围：L18-L37 / L68-L78
  - 片段概述：失败率低于阈值触发 `MetricDisableChannel`。

# 结论
- 渠道健康状态既可被“错误语义”驱动禁用，也可被“成功率指标”熔断。

